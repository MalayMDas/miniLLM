# Core (local-runnable pieces)
torch>=2.2
numpy
pyyaml
tokenizers>=0.15        # byte-level BPE trainer
datasets>=2.18          # HuggingFace streaming (optional for smoke-train)
tqdm
tensorboard>=2.15       # local experiment tracking / progress dashboard

# Optional, per feature (install when needed):
# serving API:        fastapi uvicorn pydantic
# pretrained vision:  transformers            (SigLIP toggle in vision/encoder.py)
# scale-up / cloud:   accelerate deepspeed flash-attn trl lm-eval vllm bitsandbytes
# better RAG:         sentence-transformers faiss-cpu chromadb
# hosted tracking:    wandb
