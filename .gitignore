# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/

# Virtual envs
.venv/
venv/
env/

# Experiment tracking & training artifacts (large / reproducible — keep out of git)
# TensorBoard event files:
runs/
wandb/
# trained tokenizers, checkpoints:
artifacts/
checkpoints/
*.ckpt
*.pt
*.bin
*.safetensors
*.gguf
*.bin.meta.json

# Data (pull from HF / regenerate; don't bloat the repo)
data/cache/
data/*.arrow
*.tfrecord
# generated post-training data (real prepped datasets — keep the small sample_* fixtures)
data/instruct.jsonl
data/tools.jsonl
data/reason.jsonl

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Secrets / cloud
.env
*.env
*.key
service-account*.json
